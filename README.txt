CountingSizeOfName
- This program calculates the length/size of the given name in Java. This also disregards the space (" ") found in the String and outputs only the values found in the alphabeth (both Capital and Lower Case).

ReverseString
- This program reverses a String using two methods (documentation found inside the code).